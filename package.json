{
  "name": "xxii-driver-retention",
  "private": true,
  "version": "0.1.0",
  "description": "XXII Driver Retention module (Next.js, mock GP)",
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "start": "npm run start --prefix frontend"
  }
}
